library(bnlearn)

for (net in system("ls -1 *.rda", intern = TRUE)) {

  load(net)
  fitted = bn
  cat("[[", net, "]]\n")

  for (i in 1:2) {

    data = rbn(fitted, n = 100)
    global.markov.switch = TRUE
    learned.markov = mmpc(data, debug = TRUE)

  }

}
